---
# Handlers so rodam se alguma task com "notify" reportar changed.
# Assim o daemon nao e reiniciado a toa em execucoes repetidas.
- name: Reiniciar docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true
